## webserver PC 恢复菜单入口 + 集成 sdkwork-deployments 域名管理

**修正方向**：菜单入口保留（控制台 Domains/Certificates 恢复原位置），页面内容改为**直接复用 sdkwork-deployments PC 的域名管理组件**（跨仓库 workspace 依赖，非复制代码、非跳转）。

### 1. 跨仓库 workspace 链接（webserver/pnpm-workspace.yaml，沿用既有 ../sdkwork-iam、../sdkwork-drive 模式）
- `../sdkwork-deployments/apps/sdkwork-deployments-pc/packages/*`（deploy PC 的 commons/console-core/console-delivery）
- `../sdkwork-deployments/sdks/sdkwork-deployments-app-sdk/sdkwork-deployments-app-sdk-typescript`（deploy SDK）
- webserver PC package.json 加依赖：`@sdkwork/deployments-pc-console-delivery`、`@sdkwork/deployments-pc-console-core`、`@sdkwork/deployments-pc-commons`、`@sdkwork/deployments-app-sdk`（drive-app-sdk/sdk-common 已有）

### 2. 新建包 `sdkwork-webserver-pc-console-delivery`（恢复原包名与菜单位置）
- materialize 脚本加回 console-delivery 定义：module entries `domains`（web.sites.write）、`certificates`（web.certificates.read），deps 含 deploy 包，extraIndexExports 导出集成组件
- 手写集成组件 `DeployDomainManagementSurface`（src/）：
  - props：locale、deployBaseUrl、driveBaseUrl、tokenManager
  - `createDeploymentsConsoleClients({ deployBaseUrl, driveBaseUrl, tokenManager })`（与 deploy PC 完全相同的客户端工厂，token 同 IAM 双 token 兼容）→ `<DeploymentsConsoleProvider>` → 按 resource 渲染 `DomainManagementPage`（/console/domains）或 `CertificateManagementPage`（/console/certificates）
  - deploy 组件内部硬编码 `/console/domains`、`/console/certificates` 路由与原菜单位置天然对齐，子路由（:zoneId）、证书跳转全部生效

### 3. 运行时配置
- webserver PC core 的 `loadWebserverPcRuntimeConfig` 新增 `deployAppApiBaseUrl` 字段（standalone 分支同 origin）
- `runtime-env.development.json` 加 `deployAppApiBaseUrl: "http://127.0.0.1:3900"`（deploy 后端）；其他 profile 默认 origin

### 4. surfaces 接线
- `WebserverAuthorizedWorkspace.tsx`：consoleModules 恢复 deliveryModule；构造 `resourceRenderers`（domains/certificates → 集成组件，注入 runtime.config.deployAppApiBaseUrl、runtime.config.driveAppApiBaseUrl、runtime.tokenManager、locale）传给 WebserverConsoleShell（WebserverWorkspace 已有 resourceRenderers prop，透传即可）

### 5. 样式隔离（不污染 webserver 全局样式）
- 复制 deploy PC `styles.css` 全部规则 → 机械加 `.deploy-domains-surface` 作用域前缀（node 脚本处理）→ 作为 webserver PC 新 css 文件
- 集成组件外层 `<div className="deploy-domains-surface">`；.dialog/.form-grid 等同名类与 webserver 全局隔离

### 6. 测试
- `console-workspace.test.tsx`：恢复 /console/domains、/console/certificates 导航用例（heading 断言适配 deploy 页面标题）；mock fetch 提供空列表响应
- 其余已清理测试保持

### 7. 验证
- `pnpm typecheck`（不新增错误；既有 `page` cursor 分页 WIP 遗留不属本次范围）
- `pnpm test`（vitest 全绿）
- `node tools/materialize_webserver_pc.mjs` 再生成后核对

### 不做
- 不动 deploy 仓库任何代码（纯复用）
- 不恢复 webserver 自己的域名实现（web 后端 API 仍保留，前端不再调用）
- 管理台 root-domains 入口不恢复（域名管理统一在控制台 Domains 入口，避免双入口）