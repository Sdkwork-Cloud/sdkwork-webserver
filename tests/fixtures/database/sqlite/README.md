# SQLite Repository Fixture

This directory contains test-only SQLite DDL for SQLx repository parity. It is not an SDKWork
database module, an authoritative server lifecycle root, a deployment profile, or a release asset.
